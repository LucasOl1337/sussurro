# venv do projeto: .venv (uv venv --python 3.11; uv pip install -r requirements.txt)
# Windows e Linux (wheels NVIDIA distintos por plataforma; soundcard = WASAPI / Pulse).
faster-whisper>=1.2
sounddevice>=0.5
numpy
pynput>=1.8
customtkinter>=6.0
pillow
soundcard>=0.4
